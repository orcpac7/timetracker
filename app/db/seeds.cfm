<cfscript>
	// Seed data for the time tracker.
	// Project (billing) codes will be seeded here once the projectcodes table exists.
	// Example (added in a later step):
	// seedOnce(modelName="ProjectCode", uniqueProperties="code", properties={
	//     code: 1001, description: "Internal - Admin", color: "##6b7280", active: true
	// });
</cfscript>
